					<ul>
						<li class='feed-row' id='feed-0' data-id='0' data-name='All Items'>
							Unread Items <small>({{total_unread_count}})</small>
						</li>
						{%- for feed in feeds %}
						<li class='feed-row{% if not feed.success %} error{% endif %}' id='feed-{{feed.id}}' data-id='{{feed.id}}' data-name='{{feed.title}}'{% if not feed.success %} title='{{feed.last_error}}'{% endif %}>
							<span>{{feed.title}}</span> <small>({{individual_unread_count[feed.id]|default('0')}})</small>
						</li>
						{%- endfor %}
					</ul>